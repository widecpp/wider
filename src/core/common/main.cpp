#include "main.hpp"
#include <string>
#include "../../ui/window/common/window.hpp"

int wider::core::wider_main(int argc, char *argv[])
{
    auto window = wider::ui::window::Window::create(700, 700);
    return 0;
}