#include "provider.hpp"
#include <toml.hpp>

using namespace wider::config::system;
using namespace wider::config::system::data;

Data SystemConfigProvider::load()
{
    // TODO: Load config
    return {};
}

void SystemConfigProvider::save(const Data &data)
{
    // TODO: Save config
}